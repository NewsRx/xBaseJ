rootProject.name = 'xBaseJ'

/**String[] p = ["NewsRxUI", "SHARED-JSON", "SHARED-DAO", "SHARED-POJOS", "DaoCore"]
p.each { s->
	include (":"+s);
	project(":"+s).projectDir=new File(settingsDir,"../../"+s+"/"+s)
}**/
